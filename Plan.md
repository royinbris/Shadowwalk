# Shadowing Pro - 학습용 비디오 플레이어 설계 문서 (Plan.md)

## 1. 프로젝트 개요 (Overview)
이 앱은 YouTube 또는 로컬 동영상을 활용하여 외국어 학습(쉐도잉)을 극대화하기 위해 설계된 **고성능 학습 도구**입니다. 일반적인 플레이어와 달리, 미세한 구간 반복 컨트롤과 사용자의 목소리를 즉각적으로 녹음/비교할 수 있는 오디오 엔진이 결합된 것이 특징입니다.

## 2. 주요 기능 (Core Features)
### A. 비디오 플레이어 엔진
*   **유튜브 통합**: YouTube IFrame API를 통한 정밀한 재생 제어 (속도, 구간 이동, 상태 감지).
*   **로컬 비디오 지원**: 사용자가 가진 `.mp4` 등 영상 파일을 직접 업로드하여 학습 가능.
*   **재생 속도 제어**: 0.5x ~ 2.0x까지 0.1 단위 정밀 조절.

### B. 구간 반복 및 루프 (Loop System)
*   **A-B 구간 리피트**: 시작점(A)과 끝점(B)을 설정하여 특정 문장만 반복 학습.
*   **루프 카운트**: 설정된 횟수만큼 반복 후 자동 정지 또는 다음 구간 대기.

### C. 프로급 녹음 및 비교 시스템 (PTT 스타일)
*   **Push-To-Talk (PTT)**: 버튼을 누르는 동안 녹음, 떼는 순간 즉시 내 목소리 재생.
*   **즉각적인 비교**: 내 목소리가 재생되는 동안 동영상은 자동으로 일시정지되며, 음성 재생이 끝나면 동영상이 자동으로 다시 시작됨.

## 3. 핵심 기술 로직 (Technical Logic)
### A. iOS Safari 최적화 (가장 중요)
iOS의 엄격한 보안 정책(오디오 세션 인터럽트 및 자동 재생 제한)을 극복하기 위한 로직:
*   **Zero-Latency Pause**: 사용자가 화면을 터치하는 즉시(첫 번째 `await` 발생 전), 동기적으로 `player.pauseVideo()`를 실행하여 브라우저의 '사용자 제스처' 유효성을 확보함.
*   **Activation Token (Poke)**: `onPointerDown` 시점에 유튜브를 미세하게 `play()` 시켰다가 즉시 `pause()` 하여 '재생 허가 토큰'을 확보함 (YouTube는 `onStateChange` 감지 방식 사용, 로컬 비디오는 `Promise` 기반 사용).
*   **Persistent Stream**: 녹음 패널이 열려 있는 동안 마이크 스트림을 유지하여 세션 전환 시 발생하는 유튜브 정지 현상을 방지함.

### B. 오디오 재생 엔진 (Web Audio API)
*   **BufferSource 활용**: 지연 시간을 최소화하기 위해 HTML5 오디오 대신 `AudioBuffer`와 `BufferSource`를 사용.
*   **Instant Restart**: 이전 소스의 `onended` 핸들러를 `null`로 만든 후 `stop()` 하여 연타 시 충돌을 방지하고 즉각적으로 첫 음절부터 다시 재생함.
*   **Context Management**: 재생 직전 `AudioContext.state`가 `suspended`인 경우 `resume()`을 호출하여 무음 문제를 해결함.

## 4. UI/UX 디자인 가이드 (Design Guide)
*   **테마**: 몰입감을 위해 `Zinc-950` 배경의 다크 모드 지향. 핵심 액센트 컬러는 `Yellow-500` (진행/상태) 및 `Red-500` (녹음).
*   **반응형 레이아웃**:
    *   **데스크톱**: 대화면 비디오와 우측 컨트롤 패널의 2열 구조.
    *   **모바일**: 하단 고정형 녹음 패널(PTT)과 제스처 중심의 인터페이스.
*   **피드백 시스템**:
    *   녹음/재생 시 원형 펄스(Pulse) 애니메이션을 통해 시각적 상태 전달.
    *   초 단위 카운트다운 타이머로 내 목소리 재생 길이 표시.

## 5. 단축키 시스템 (Keyboard Shortcuts)
맥북 등 데스크톱 사용자를 위한 강력한 단축키 매핑 (한글 자자 대응 포함):
*   **B (Begin)**: 녹음 시작 (ㅠ)
*   **N (Next/End)**: 녹음 종료 및 생성 (ㅜ)
*   **V (Voice Play)**: 내 목소리 재생 (ㅍ)
*   **\ (Backslash)**: 녹음 패널 토글
*   **[ / ]**: 루프 횟수 조절
*   **- / +**: 재생 속도 조절
*   **; / '**: 자막/영상 크기 조절
*   **, / .**: 반복 구간 간격 조절

## 6. 오용 방지 및 자원 관리
*   **Cleanup**: 컴포넌트 언마운트 또는 패널 종료 시 마이크 스트림을 명시적으로 `stop()` 하여 배터리 소모 및 보안 표시등(주황색 점) 관리.
*   **MIME Type**: 환경에 따라 `audio/mp4`와 `audio/webm`을 자동 감지하여 기기 호환성 확보.
